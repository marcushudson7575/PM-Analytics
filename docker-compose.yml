version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: pm-analytics-db
    environment:
      POSTGRES_USER: pmanalytics
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: pm_analytics
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pmanalytics"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
